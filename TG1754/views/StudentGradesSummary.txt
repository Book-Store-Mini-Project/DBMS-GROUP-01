CREATE OR REPLACE VIEW StudentGradesSummary AS
SELECT
    student_registration,
    course_code,
    grade
FROM StudentGrades
ORDER BY
    academic_year,
    batch_name,
    student_registration,
    course_code;
